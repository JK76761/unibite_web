# Local development only. Production can leave this empty and use same-origin API calls.
VITE_API_BASE_URL=http://localhost:5250
# Optional future enhancement only. UniBite does not call Google Places automatically.
VITE_GOOGLE_PLACES_API_KEY=
